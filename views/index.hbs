<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-KK94CHFLLe+nY2dmCWGMq91rCGa5gtU4mk92HdvYe+M/SXH301p5ILy+dN9+nJOZ" crossorigin="anonymous">
    <title>LOGIN</title>
    <style>
        #register{
            color: blue;
        }
        .nav-link{
            transition: all 0.5s;
        }
        #home{
            position: absolute;
            right: 5%;
            top: 2%;
        }
        .nav-link:hover{
            color: cyan!important;
            text-decoration: underline;
            animation: shakeYoBooty 0.5s;
        }
        @keyframes shakeYoBooty {
            0%{
                transform:translateX(0);
            }
            33.33%{
                transform:translateX(2px);
            }
            66.66%{
                transform:translateX(-4px);
            }
            100%{
                 transform:translateX(0);
            }
        }
         @media screen and (min-width: 1000px){
            .card{
                width: 50%;
            }
        }
    </style>
</head>
<body class="text-bg-dark">
    <div id="home" class="border p-3 rounded-2 text-white">
        <a href="/" class="nav-link">Home</a>
    </div>
    <div class="container">
        <div class="card w-50 mx-auto mt-5 border border-2 border-primary text-bg-secondary">
            <div class="card-header text-center">Kodego Login</div>
            <div class="card-body">
                {{#if message}}
                    <h4 class="alert alert-info mt-4 text-center">{{message}}</h4>
                {{/if}}
                <form action="/auth/login" method="post">
                    <label for="email" class="form-label">Email</label>
                    <input type="email" name="email" id="email" class="form-control mb-3">
                    <label for="password" class="form-label">Password</label>
                    <input type="text" name="password" id="password" class="form-control mb-3">
                    <button type="submit" class="btn btn-primary">Submit</button>
                </form>
                <div class="text-center">
                    <p class="nav-item">No account yet? <a href="/register" class="nav-link d-inline-block" id="register">Register here!</a></p>
                </div>
            </div>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ENjdO4Dr2bkBIFxQpeoTz1HIcje39Wm4jDKdf19U8gI4ddQ3GYNS7NTKfAdVQSZe" crossorigin="anonymous"></script>
</body>
</html>